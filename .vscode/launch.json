{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "DEBUG",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/watch-docker/main.go",
            "env": {},
            "cwd": "${workspaceFolder}"
        }
    ]
}